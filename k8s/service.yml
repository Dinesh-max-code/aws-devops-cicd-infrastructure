apiVersion: v1
kind: Service

metadata:
  name: aws-devops-cicd-service
  namespace: default

spec:
  selector:
    app: aws-devops-cicd

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 31514

  type: NodePort



  
